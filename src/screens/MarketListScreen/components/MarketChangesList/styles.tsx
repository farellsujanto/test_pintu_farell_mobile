import { StyleSheet } from 'react-native';
import { greyColor } from '../../../../themes/colors';

export default StyleSheet.create({
    cardSeparator: {
        orderBottomColor: greyColor,
        borderBottomWidth: StyleSheet.hairlineWidth,
    },
});
